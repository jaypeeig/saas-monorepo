{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "name": "saas-monorepo",
  "packageManager": "yarn@1.19.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
