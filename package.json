{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --no-daemon",
    "test": "turbo run test --no-daemon",
    "test:ui": "turbo run test --filter=@saas/ui --no-daemon",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "name": "saas-monorepo",
  "packageManager": "yarn@1.19.2",
  "workspaces": [
    "apps/**",
    "packages/**"
  ]
}
